# Custom Agent Rules

## Project-Specific Guidelines

Add your custom rules here to guide the agent's behavior for this project.

### Code Style
- Example: Always use single quotes for strings
- Example: Prefer functional components over class components

### Architecture Patterns
- Example: Follow the MVC pattern
- Example: Keep components in `/components` directory

### Testing Requirements
- Example: All new features must include unit tests
- Example: Run tests before committing

### Documentation Standards
- Example: Add JSDoc comments for all public functions
- Example: Update README when adding new features

## Communication Preferences
- Example: Always ask before making breaking changes
- Example: Provide detailed explanations for complex refactoring
